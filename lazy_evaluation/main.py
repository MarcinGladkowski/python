"""
Two types of evaluation:
1. Eager evaluation
2. Lazy evaluation (call-by-needed)
"""

for index in range(1, 1_000_001):
    """
        Other examples: zip(), enumerate() ...
        Lazy evaluation with loop and range()
        We do not need to keep all values generated by range() at once in memory
    """
    print(f"This is iteration {index}")